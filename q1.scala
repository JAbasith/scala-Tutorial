object DiskArea{
    def main(args: Array[String]): Unit ={
        val radius = 5
        val area = math.Pi*radius*radius
        
        println(s"Area of the disk with radius $radius is $area.")
    }
}